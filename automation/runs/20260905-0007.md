# 2026-09-05 00:07 PKT — Phase 4

## Tasks
1. Portable pack launchers now wait for `/health` (`Wait-And-Open.ps1`) instead of a 2s timeout.
2. `launch_engine.pick_bind`: reuse a healthy engine on 8787 or take the next free port; write `.agentforge/engine.port`.
3. NSIS source + build script: `desktop/windows/agentforge.nsi`, `build_installer.ps1`, `Install-Shortcuts.ps1`.

## Tests
`pytest tests/test_launch_engine.py tests/test_pack_portable.py tests/test_windows_nsi.py` — 10 passed.

## Phase
Still Phase 4. Exit needs a compiled `AgentForge-Setup.exe` on a Windows box (`makensis` or Tauri NSIS).
