# 2026-09-04 23:05 PKT — Phase 4

## Tasks
1. Windows double-click path without NSIS: `desktop/windows/Start-AgentForge.bat` + `Wait-And-Open.ps1`.
2. Portable packer `desktop/pack_portable.py` (backend/src/frontend/skills/config/knowledge/desktop + launchers). Tests in `tests/test_pack_portable.py`.
3. Tauri: set PYTHONPATH when spawning uvicorn; after `/health` succeed, load live `http://127.0.0.1:8787/ui/` in the main window. `launch_engine._ui_url`.

## Not done
- NSIS/MSI binary (needs Windows + Rust + WebView2). Phase 4 exit still open.

## Checks
- pytest tests/test_launch_engine.py tests/test_pack_portable.py — 6 passed
