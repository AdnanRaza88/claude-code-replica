# 2026-09-04 22:08 PKT — Phase 4

## Tasks
1. First-run settings panel in `frontend/` (overlay + Settings button). Saves via PUT `/settings`. Skip marks `first_run_done`.
2. `AppSettings.first_run_done` + unit coverage in `tests/test_settings.py`.
3. Tauri system tray: close hides the main window; tray menu Show / Quit (Quit kills uvicorn). Feature `tray-icon`. Window label `main`.

## Not done
- NSIS/MSI binary (needs Windows + Rust + WebView2). Phase 4 exit still open.

## Checks
- SettingsService first_run + mask path exercised locally.
- Full pytest suite not run here (fastapi not installed in this sandbox).
