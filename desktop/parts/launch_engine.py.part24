item.get('id')} {item.get('action')}", flush=True)
        print("onboard-ok" if report.get("ok") else "onboard-fail", flush=True)
        return 0 if report.get("ok") else 1

    if args.windows_smoke:
        from src.services.desktop_status import windows_smoke_packet, write_windows_smoke_report

        report = windows_smoke_packet()
        path = write_windows_smoke_report(report)
        print(f"windows-smoke-report {path}", flush=True)
        print(f"next {report.get('next')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"platform {report.get('platform')}", flush=True)
        print(f"windows_host {report.get('windows_host')}", flush=True)
        print(f"accepted {report.get('windows_accepted')}", flush=True)
        print(f"smoke_passed {report.get('smoke_passed')}", flush=True)
        print(f"exit_met {report.get('exit_met')}", flush=True)
        for item in report.get("checklist") or []:
            print(f"check {item}", flush=True)
        print("windows-smoke-ok" if report.get("ok") else "windows-smoke-fail", flush=True)
        return 0 if report.get("ok") else 1

    if args.gate:
        from src.services.desktop_status import desktop_status, write_gate_report

        report = desktop_status()
        path = write_gate_report(report)
        print(f"gate-report {path}", flush=True)
        print(f"next {report.get('next')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"platform {report.get('platform')}", flush=True)
        print(f"windows_host {report.get('windows_host')}", flush=True)
        print(f"accepted {report.get('windows_accepted')}", flush=True)
        print(f"exit_met {report.get('exit_met')}", flush=True)
        for item in report.get("remaining") or []:
            print(f"remaining {item}", flush=True)
        print("gate-ok" if report.get("ok") else "gate-fail", flush=True)
        return 0 if report.get("ok") else 1

    if args.closeout:
        report = closeout_packet(args.host, args.port)
        path = write_closeout_report(report)
        print(f"closeout-report {path}", flush=True)
        print(f"next {report.get('next')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"platform {report.get('platform')}", flush=True)
        print(f"windows_host {report.get('windows_host')}", flush=True)
        print(f"accepted {report.get('accepted')}", flush=True)
        print(f"exit_met {report.get('exit_met')}", flush=True)
        for item in report.get("remaining") or []:
            print(f"remaining {item}", flush=True)
        print("closeout-ok" if report["ok"] else "closeout-fail", flush=True)
        return 0 if report["ok"] else 1

    if args.operator:
        report = operator_packet(args.host, args.port)
        path = write_operator_report(report)
        print(f"operator-report {path}", flush=True)
        print(f"next {report.get('next')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"accepted {report.get('accepted')}", flush=True)
        print(f"exit_met {report.get('exit_met')}", flush=True)
        print(f"workflow {report.get('workflow')}", flush=True)
        for i, step in enumerate(report.get("steps") or [], start=1):
            print(f"{i}. {step}", flush=True)
        print("operator-ok" if report["ok"] else "operator-fail", flush=True)
        return 0 if report["ok"] else 1

    if args.release:
        report = release_packet(args.host, args.port)
        path = write_release_report(report)
        print(f"release-report {path}", flush=True)
        print(f"next {report.get('next')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"accepted {report.get('accepted')}", flush=True)
        print(f"exit_met {report.get('exit_met')}", flush=True)
        print(f"workflow {report.get('workflow')}", flush=True)
        for name in report.get("artifacts") or []:
            print(f"artifact {name}", flush=True)
        for i, step in enumerate(report.get("steps") or [], start=1):
            print(f"{i}. {step}", flush=True)
        print("release-ok" if report["ok"] else "release-fail", flush=True)
        return 0 if report["ok"] else 1

    if args.accept_windows:
        stamp = write_windows_stamp()
        print(f"windows-accepted {stamp}", flush=True)

    if args.acceptance or args.accept_windows:
        report = acceptance_packet(args.host, args.port)
        path = write_acceptance_report(report)
        write_signoff_report(signoff_packet(args.host, args.port))
        print(f"acceptance-report {path}", flush=True)
        print(f"next {report.get('next')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"accepted {report.get('accepted')}", flush=True)
        print(f"exit_met {report.get('exit_met')}", flush=True)
        print(f"silent {report.get('silent')}", flush=True)
        for check in report.get("criteria") or []:
            mark = "ok" if check.get("ok") else "open"
            print(f"{mark:4} {check['id']:28} {check['detail']}", flush=True)
        print("acceptance-ok" if report["ok"] else "acceptance-fail", flush=True)
        return 0 if report["ok"] else 1

    if args.signoff:
        report = signoff_packet(args.host, args.port)
        path = write_signoff_report(report)
        print(f"signoff-report {path}", flush=True)
        print(f"next {report.get('next')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"exit_met {report.get('exit_met')}", flush=True)
        print(f"silent {report.get('silent')}", flush=True)
        for check in report.get("criteria") or []:
            mark = "ok" if check.get("ok") else "open"
            print(f"{mark:4} {check['id']:28} {check['detail']}", flush=True)
        print("signoff-ok" if report["ok"] else "signoff-fail", flush=True)
        return 0 if report["ok"] else 1

    if args.handoff:
        report = handoff_packet(args.host, args.port)
        path = write_handoff_report(report)
        print(f"handoff-report {path}", flush=True)
        print(f"next {report.get('next')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"silent {report.get('silent')}", flush=True)
        for i, step in enumerate(report.get("steps") or [], start=1):
            print(f"{i}. {step}", flush=True)
        print("handoff-ok" if report["ok"] else "handoff-fail", flush=True)
        return 0 if report["ok"] else 1

    if args.verify_install:
        report = verify_install()
        path = write_verify_report(report)
        print(f"verify-report {path}", flush=True)
        print(f"silent {report.get('silent')}", flush=True)
        for check in report["checks"]:
            mark = "ok" if check["ok"] else "fail"
            print(f"{mark:4} {check['id']:28} {check['detail']}", flush=True)
        print("verify-ok" if report["ok"] else "verify-fail", flush=True)
        return 0 if report["ok"] else 1

    if args.preflight:
        report = preflight_check(args.host, args.port)
        path = write_preflight_report(report)
        print(f"preflight-report {path}", flush=True)
        print(f"next {report.get('next')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"silent {report.get('silent')}", flush=True)
        for check in report["checks"]:
            mark = "ok" if check["ok"] else "fail"
            print(f"{mark:4} {check['id']:28} {check['detail']}", flush=True)
        print("preflight-ok" if report["ok"] else "preflight-fail", flush=True)
        return 0 if report["ok"] else 1

    if args.guide:
        report = guide_check(args.host, args.port)
        path = write_guide_report(report)
        print(f"guide-report {path}", flush=True)
        print(f"next {report.get('next')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"silent {report.get('silent')}", flush=True)
        for i, st