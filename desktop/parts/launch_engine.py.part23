t(f"accepted {report.get('windows_accepted')}", flush=True)
        print(f"blocked_on_linux {report.get('blocked_on_linux')}", flush=True)
        print(f"exit_met {report.get('exit_met')}", flush=True)
        for item in report.get("steps") or []:
            print(f"step {item.get('id')} {item.get('command')}", flush=True)
        print("windows-host-ok" if report.get("ok") else "windows-host-fail", flush=True)
        return 0 if report.get("ok") else 1

    if args.ci_go:
        from src.services.desktop_status import ci_go_packet, write_ci_go_report

        report = ci_go_packet()
        path = write_ci_go_report(report)
        print(f"ci-go-report {path}", flush=True)
        print(f"health {report.get('health_url')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"platform {report.get('platform')}", flush=True)
        print(f"windows_host {report.get('windows_host')}", flush=True)
        print(f"accepted {report.get('windows_accepted')}", flush=True)
        print(f"complete {report.get('complete')}", flush=True)
        print(f"exit_met {report.get('exit_met')}", flush=True)
        for item in report.get("steps") or []:
            print(f"step {item.get('id')} {item.get('bat')} {item.get('flag')}", flush=True)
        print("ci-go-ok" if report.get("ok") else "ci-go-fail", flush=True)
        return 0 if report.get("ok") else 1

    if args.ci_verify:
        from src.services.desktop_status import ci_verify_packet, write_ci_verify_report

        report = ci_verify_packet()
        path = write_ci_verify_report(report)
        print(f"ci-verify-report {path}", flush=True)
        print(f"health {report.get('health_url')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"platform {report.get('platform')}", flush=True)
        print(f"windows_host {report.get('windows_host')}", flush=True)
        print(f"accepted {report.get('windows_accepted')}", flush=True)
        print(f"verified {report.get('verified')}", flush=True)
        print(f"exit_met {report.get('exit_met')}", flush=True)
        for item in report.get("commands") or []:
            print(f"command {item}", flush=True)
        print("ci-verify-ok" if report.get("ok") else "ci-verify-fail", flush=True)
        return 0 if report.get("ok") else 1

    if args.ci_install:
        from src.services.desktop_status import ci_install_packet, write_ci_install_report

        report = ci_install_packet()
        path = write_ci_install_report(report)
        print(f"ci-install-report {path}", flush=True)
        print(f"source {report.get('source_dir')}", flush=True)
        print(f"silent {report.get('silent')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"platform {report.get('platform')}", flush=True)
        print(f"windows_host {report.get('windows_host')}", flush=True)
        print(f"accepted {report.get('windows_accepted')}", flush=True)
        print(f"exit_met {report.get('exit_met')}", flush=True)
        for item in report.get("commands") or []:
            print(f"command {item}", flush=True)
        for item in report.get("artifacts") or []:
            print(f"artifact {item}", flush=True)
        print("ci-install-ok" if report.get("ok") else "ci-install-fail", flush=True)
        return 0 if report.get("ok") else 1

    if args.ci_pull:
        from src.services.desktop_status import ci_pull_packet, write_ci_pull_report

        report = ci_pull_packet()
        path = write_ci_pull_report(report)
        print(f"ci-pull-report {path}", flush=True)
        print(f"target {report.get('target_dir')}", flush=True)
        print(f"workflow {report.get('workflow')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"platform {report.get('platform')}", flush=True)
        print(f"windows_host {report.get('windows_host')}", flush=True)
        print(f"accepted {report.get('windows_accepted')}", flush=True)
        print(f"exit_met {report.get('exit_met')}", flush=True)
        for item in report.get("commands") or []:
            print(f"command {item}", flush=True)
        for item in report.get("artifacts") or []:
            print(f"artifact {item}", flush=True)
        print("ci-pull-ok" if report.get("ok") else "ci-pull-fail", flush=True)
        return 0 if report.get("ok") else 1

    if args.ci_watch:
        from src.services.desktop_status import ci_watch_packet, write_ci_watch_report

        report = ci_watch_packet()
        path = write_ci_watch_report(report)
        print(f"ci-watch-report {path}", flush=True)
        print(f"actions {report.get('actions_url')}", flush=True)
        print(f"workflow {report.get('workflow')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"platform {report.get('platform')}", flush=True)
        print(f"windows_host {report.get('windows_host')}", flush=True)
        print(f"accepted {report.get('windows_accepted')}", flush=True)
        print(f"exit_met {report.get('exit_met')}", flush=True)
        for item in report.get("commands") or []:
            print(f"command {item}", flush=True)
        for item in report.get("artifacts") or []:
            print(f"artifact {item}", flush=True)
        print("ci-watch-ok" if report.get("ok") else "ci-watch-fail", flush=True)
        return 0 if report.get("ok") else 1

    if args.ci_trigger:
        from src.services.desktop_status import ci_trigger_packet, write_ci_trigger_report

        report = ci_trigger_packet()
        path = write_ci_trigger_report(report)
        print(f"ci-trigger-report {path}", flush=True)
        print(f"command {report.get('command')}", flush=True)
        print(f"workflow {report.get('workflow')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"platform {report.get('platform')}", flush=True)
        print(f"windows_host {report.get('windows_host')}", flush=True)
        print(f"accepted {report.get('windows_accepted')}", flush=True)
        print(f"exit_met {report.get('exit_met')}", flush=True)
        for item in report.get("artifacts") or []:
            print(f"artifact {item}", flush=True)
        print("ci-trigger-ok" if report.get("ok") else "ci-trigger-fail", flush=True)
        return 0 if report.get("ok") else 1

    if args.windows_path:
        from src.services.desktop_status import windows_path_packet, write_windows_path_report

        report = windows_path_packet()
        path = write_windows_path_report(report)
        print(f"windows-path-report {path}", flush=True)
        print(f"next {report.get('next')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"workflow {report.get('workflow')}", flush=True)
        print(f"platform {report.get('platform')}", flush=True)
        print(f"windows_host {report.get('windows_host')}", flush=True)
        print(f"accepted {report.get('windows_accepted')}", flush=True)
        print(f"exit_met {report.get('exit_met')}", flush=True)
        for item in report.get("steps") or []:
            print(f"step {item.get('id')} {item.get('action')}", flush=True)
        print("windows-path-ok" if report.get("ok") else "windows-path-fail", flush=True)
        return 0 if report.get("ok") else 1

    if args.onboard:
        from src.services.desktop_status import onboard_packet, write_onboard_report

        report = onboard_packet()
        path = write_onboard_report(report)
        print(f"onboard-report {path}", flush=True)
        print(f"next {report.get('next')}", flush=True)
        print(report.get("hint") or "", flush=True)
        print(f"platform {report.get('platform')}", flush=True)
        print(f"windows_host {report.get('windows_host')}", flush=True)
        print(f"accepted {report.get('windows_accepted')}", flush=True)
        print(f"exit_met {report.get('exit_met')}", flush=True)
        for item in report.get("steps") or []:
            print(f"step {